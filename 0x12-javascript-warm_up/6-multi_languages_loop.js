#!/usr/bin/node

const print = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
let iteration = 0;

for (iteration; iteration < print.length; iteration++) {
  console.log(print[iteration]);
}
